---
phase: 01-security
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/migrate-now/route.ts
  - src/proxy.ts
autonomous: true
requirements:
  - SEC-02

must_haves:
  truths:
    - "The file src/app/api/migrate-now/route.ts does not exist"
    - "PUBLIC_PATHS in src/proxy.ts does not contain /api/migrate-now"
    - "Unauthenticated requests to /api/migrate-now receive a 404 (route gone) rather than executing"
  artifacts:
    - path: "src/proxy.ts"
      provides: "Middleware without migrate-now in PUBLIC_PATHS"
      contains: "PUBLIC_PATHS"
  key_links:
    - from: "src/proxy.ts"
      to: "PUBLIC_PATHS array"
      via: "string removal"
      pattern: "PUBLIC_PATHS.*login.*register.*api/auth"
---

<objective>
Delete the unauthenticated /api/migrate-now endpoint entirely.

Purpose: /api/migrate-now was listed in PUBLIC_PATHS, meaning any unauthenticated caller could trigger arbitrary SQL execution against the production database. The route must be deleted and its PUBLIC_PATHS entry removed. No auth guard, no stub — the attack surface is eliminated by deletion.

Output:
- src/app/api/migrate-now/route.ts: deleted
- src/proxy.ts: /api/migrate-now removed from PUBLIC_PATHS array
</objective>

<execution_context>
@C:/Users/joseph/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/joseph/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- Key patterns the executor needs. Extracted from codebase. -->

From src/proxy.ts line 4 (current — to be edited):
```typescript
const PUBLIC_PATHS = ['/login', '/register', '/api/auth/register', '/api/auth/login', '/api/migrate-now']
```

Target (after removal):
```typescript
const PUBLIC_PATHS = ['/login', '/register', '/api/auth/register', '/api/auth/login']
```

The route file to delete:
- src/app/api/migrate-now/route.ts
- No other files in the codebase reference or import this route (confirmed by research)
- The entire directory src/app/api/migrate-now/ can be removed
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Delete the migrate-now route file</name>
  <files>src/app/api/migrate-now/route.ts</files>
  <action>
Delete the file src/app/api/migrate-now/route.ts. Also remove the now-empty directory src/app/api/migrate-now/ if it exists.

Use the filesystem — do not replace the file with a stub or 404 handler. The goal is zero attack surface: no file, no route handler, no response.

After deletion, verify the file is gone:
```
test ! -f src/app/api/migrate-now/route.ts && echo "PASS: file deleted"
```
  </action>
  <verify>
    <automated>test ! -f C:/Users/joseph/.openclaw/workspace/HelpDash/src/app/api/migrate-now/route.ts && echo "PASS: route file deleted" || echo "FAIL: file still exists"</automated>
  </verify>
  <done>src/app/api/migrate-now/route.ts does not exist on disk. The directory src/app/api/migrate-now/ is also absent.</done>
</task>

<task type="auto">
  <name>Task 2: Remove /api/migrate-now from PUBLIC_PATHS in src/proxy.ts</name>
  <files>src/proxy.ts</files>
  <action>
In src/proxy.ts, edit the PUBLIC_PATHS array on line 4. Remove the string '/api/migrate-now' from the array.

Current:
```typescript
const PUBLIC_PATHS = ['/login', '/register', '/api/auth/register', '/api/auth/login', '/api/migrate-now']
```

After:
```typescript
const PUBLIC_PATHS = ['/login', '/register', '/api/auth/register', '/api/auth/login']
```

No other changes to proxy.ts. The middleware logic, cookie handling, and auth redirect behavior all remain identical.

After editing, verify the entry is gone:
```
grep "migrate-now" src/proxy.ts
```
Expected: no output.
  </action>
  <verify>
    <automated>grep "migrate-now" C:/Users/joseph/.openclaw/workspace/HelpDash/src/proxy.ts && echo "FAIL: entry still present" || echo "PASS: entry removed"</automated>
  </verify>
  <done>src/proxy.ts PUBLIC_PATHS array contains exactly four entries: /login, /register, /api/auth/register, /api/auth/login. The string /api/migrate-now is absent.</done>
</task>

</tasks>

<verification>
1. Route file gone:
```
test ! -f src/app/api/migrate-now/route.ts && echo PASS
```

2. PUBLIC_PATHS clean:
```
grep "migrate-now" src/proxy.ts
```
Expected: no output (non-zero exit).

3. No other references:
```
grep -r "migrate-now" src/
```
Expected: no output.
</verification>

<success_criteria>
- src/app/api/migrate-now/route.ts does not exist
- src/proxy.ts PUBLIC_PATHS does not contain /api/migrate-now
- grep -r "migrate-now" src/ returns zero matches
- Next.js will return 404 for any request to /api/migrate-now (no handler registered)
</success_criteria>

<output>
After completion, create `.planning/phases/01-security/01-02-SUMMARY.md`
</output>
